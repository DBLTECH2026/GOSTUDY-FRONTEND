import type { SidebarItem } from '@/shared/lib/sidebar-registry';

// Persona B — items para el módulo Inscripción (panel admin)
export const inscripcionSidebar: SidebarItem[] = [
  // { label: 'Inscripciones', href: '/inscripciones', icon: 'ClipboardList', roles: ['admin'], order: 10 },
];
