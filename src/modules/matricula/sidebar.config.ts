import type { SidebarItem } from '@/shared/lib/sidebar-registry';

// Persona B — items del módulo Matrícula
export const matriculaSidebar: SidebarItem[] = [
  // { label: 'Matrícula', href: '/matricula', icon: 'BookMarked', roles: ['admin'], order: 11 },
];
